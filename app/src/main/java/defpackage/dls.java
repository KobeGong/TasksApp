package defpackage;

/* renamed from: dls  reason: default package */
/* compiled from: PG */
public enum dls {
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    BOOLEAN,
    STRING,
    BYTE_STRING,
    ENUM,
    MESSAGE;

    static {
        Integer.valueOf(0);
        Long.valueOf(0);
        Float.valueOf(0.0f);
        Double.valueOf(0.0d);
        Boolean.valueOf(false);
    }
}
